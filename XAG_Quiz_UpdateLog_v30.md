# XAG Quiz 업데이트 내역 (v29)

## 버전 정보

| 구분 | 버전 |
|---|---|
| 퀴즈 데이터 (Excel) | **v29** |
| 퀴즈 게임 (index.html) | **v1.22** |
| 이전 버전 | 데이터 v28 / 게임 v1.3 |

> **버전 규칙**: 원칙·판단·시각 3종 프레임워크로 개선한 v1.2가 기준점이며, 이후 각 XAG 섹션을
> 이 프레임워크로 재설계할 때마다 1.2X로 순차 증가한다 (XAG103=1.22 → XAG104=1.23 → ...).

---

## 1. XAG103 퀴즈 전면 재설계

### 문제 수 및 유형 변경

| 항목 | v28 (이전) | v29 (이후) |
|---|---|---|
| 문제 수 | 33개 | 35개 |
| 원칙·정의 확인 | 33개 (100%) | 11개 (31%) |
| 판단·적용 | 0개 | 17개 (49%) |
| 설계 오류 발견 (시각) | 0개 | 7개 (20%) |

XAG101·102와 동일한 프레임워크(30/50/20)로 재설계했습니다. XAG103의 핵심 주제인
"단일 감각 채널 의존 금지 → 다중 채널(시각·청각·촉각) 보강"에 맞춰, 색각 이상·청각 장애·
촉각 미지원 기기(Xbox Adaptive Controller) 관련 판단형 시나리오를 대거 추가했습니다.

### 시각 문제 7개 구성 (신규 render_type: `cue_compare`)

| key | 시나리오 | 오류 → 개선 |
|---|---|---|
| V001 | 적 유닛 표시 | 색상 단독(빨강) → 색상+아이콘(▲)+라벨("적") |
| V002 | 구매 불가 아이템 | 회색 단독 → 회색+자물쇠 아이콘+"구매 불가" |
| V003 | 텍스트 전용 대화 화자 표시 | 표시 없음 → 화자 아이콘+이름 라벨 |
| V004 | 화면 밖 총성 | 이펙트 아이콘만 → 방향 아이콘+"후방 피격" |
| V005 | 상태 이상(디버프) | 색상 패치만 → 해골 아이콘+"중독" |
| V006 | 미니맵 퀘스트 마커 | 색상 단독(노랑) → 별 아이콘+"주요 퀘스트" |
| V007 | 업적 달성 알림 | 색상 패치만 → 트로피 아이콘+"업적 달성" |

---

## 2. 게임 앱 업데이트 (index.html v1.3 → v1.22)

### 신규 비교 렌더러 추가: `cue_compare`

XAG103(다중 감각 채널) 전용 렌더러로, 색상패치+아이콘+라벨 3요소 조합을 패널별로
독립 지정할 수 있습니다.

**visual_code 구조:**

```json
{
  "render_type": "cue_compare",
  "scenario": "상황 설명 (예: 잠긴 상점 아이템)",
  "panel_a": { "color_patch": "#e74c3c", "icon": "▲", "label": "적" },
  "panel_b": { "color_patch": "#e74c3c", "icon": null, "label": null },
  "label_a": "A",
  "label_b": "B"
}
```

- `color_patch`: 배지 배경색(hex)
- `icon`: 이모지/심볼 (없으면 `null` → 해당 채널 결여 상태를 표현)
- `label`: 텍스트 캡션 (없으면 `null`)
- `label_a/b`는 기존 힌트 노출 버그 학습을 반영해 항상 "A"/"B" 고정. 정답 힌트는
  패널 내용(`color_patch`/`icon`/`label`)에만 존재하며, 어느 패널이 A/B로 배정될지는
  문항마다 교차 배치하여 위치 패턴화를 방지함.

기존 `font_compare`/`text_align`/`caps_compare`/`dialog_compare`와 완전 하위 호환되며,
`visual_type: color_pair` + `visual_code` JSON 구조를 그대로 따릅니다.

### CSS 추가

`.cue-compare`, `.cue-scenario`, `.cue-row`, `.cue-panel`, `.cue-label`, `.cue-badge`,
`.cue-icon`, `.cue-caption` 클래스를 기존 `.compare-panel` 계열과 톤을 맞춰 추가.

---

## 3. 데이터 정합성 점검

- XAG103 신규 35행: 키 중복 없음, `visual_code` JSON 전량 파싱 검증 완료
- 전체 데이터 행수: 810 → 812 (XAG103 +2행)
- 전체 XAG 섹션 문항 수 변동: XAG103만 33→35, 나머지 섹션 변동 없음

## 4. 파일 현황

| 파일 | 설명 |
|---|---|
| `XAG_Quiz_RawData_v28.xlsx` | 보존 기준 (812행 이전, XAG103 33개) |
| `XAG_Quiz_RawData_v29.xlsx` | **현재 작업본** (812행, XAG103 35개 재설계) |
| `index.html` | **현재 작업본** (v1.22, `cue_compare` 렌더러 추가) |

## 6. v29 → v30 추가 수정: 아이콘 렌더링·대비 버그

팀 QA 스크린샷 피드백(7건) 반영. 두 가지 문제가 발견됨:

### 문제 1: 이모지 아이콘 깨짐 (tofu box 표시)
`🔒`·`🗣️`·`💥`·`☠️`·`🏆`는 서플리멘터리 플레인(U+1F300 이상) 이모지로, 컬러 이모지 폰트가
없는 환경(webview 등)에서 미지원 문자 박스(☒)로 깨짐. 반면 BMP 심볼인 `▲`·`★`·`⬅️`는
정상 렌더링됨 — 즉 특정 유니코드 대역 의존이 근본 원인.

**조치**: 아이콘 7종 전체를 폰트 글리프 의존 없는 **순수 SVG 아이콘**으로 교체
(`CUE_ICON_PATHS` + `buildCueIconSVG()`, index.html). `visual_code.icon` 필드 값도
이모지 문자열 → 아이콘 키(`triangle`/`star`/`lock`/`speech`/`arrow_left`/`impact`/`skull`/`trophy`)로 변경.

### 문제 2: 아이콘-배경 대비 부족 (예: 노란 배경 + 흰 별 아이콘)
아이콘 색이 고정값(흰색)이었어서 밝은 배경(노랑 등)에서 대비가 낮아지는 문제.
XAG 102의 "적응형 대비" 원칙을 그대로 적용하여, WCAG 상대 휘도 공식으로 배경색과
흰색/어두운색(#161a24) 각각의 대비율을 계산해 **더 높은 쪽을 자동 선택**하도록 구현
(`relativeLuminance`/`contrastRatio`/`contrastIconColor`). 추가로 `drop-shadow` 필터로
경계를 보강해 어떤 배경에서도 아이콘 윤곽이 흐려지지 않게 함.

| 배경색 | 이전(고정 흰색) 대비율 | 이후(자동 선택) | 선택 색상 |
|---|---|---|---|
| `#e74c3c` (빨강) | 3.82 | **4.55** | 어두운색 |
| `#888888` (회색) | 3.54 | **4.91** | 어두운색 |
| `#3498db` (파랑) | 3.15 | **5.52** | 어두운색 |
| `#8e44ad` (보라) | 5.87 | **5.87** | 흰색 (기존과 동일, 이미 최선) |
| `#f1c40f` (노랑) | 1.66 | **10.47** | 어두운색 (기존 문제 해결) |
| `#2ecc71` (초록) | 2.10 | **8.28** | 어두운색 |

### 파일 버전
- 데이터: v29 → **v30** (`visual_code.icon` 키 값 변경)
- 게임: v1.22 유지 (신규 섹션 재설계가 아닌 동일 버전 내 버그 수정이므로 번호 미증가)


## 7. 후속 작업 필요 사항

- `XAG_Quiz_Converter_latest.html`로 `XAG_Quiz_RawData_v30.xlsx` → `XAG_Quiz_RawData.js`
  재변환 필요 (게임이 실제로 로드하는 파일은 xlsx가 아닌 변환된 js)
- XAG104부터는 아직 미착수 (원칙·정의 100% 상태 유지 중)
