## XAG 접근성 퀴즈 - 구현 기능 전체 정리

> 기준 버전: v26 데이터 반영본  
> 최신 반영 사항: v26 RawData 873문항, 컨버터 v2, OX/주관식 정답 체크 보강, 섹션 카테고리 선택 UI, 게임 가이드 랭킹 문구 정리

---

### 1. 핵심 시스템

| 기능 | 설명 |
|------|------|
| **Google 로그인** | Firebase Auth 기반, 팝업 방식으로 PC/모바일 모두 지원 |
| **오답노트 동기화** | 서버 우선 정책 — 로그인 시 서버 데이터 로드, 세션 중 실시간 동기화 |
| **오답노트 관리** | 틀린 문제 자동 저장, 맞추면 자동 제거, 섹션별/전체 초기화 가능 |
| **Fever 시스템** | 3연속 정답 → FEVER(x2), 6연속 정답 → SUPER FEVER(x3), 오답 시 리셋 |
| **랭킹 시스템** | Firestore 기반, totalScore(Fever 보너스 및 타임어택 보너스 포함)로 순위 산정 |
| **랭킹 반영 범위 명확화** | 랜덤 10문제는 랭킹 미반영, **섹션별 전체 풀기**와 **섹션별 타임어택 전체 풀기**만 랭킹 반영 |
| **문제 타입 확장** | 4지선다(MC) + OX퀴즈 + 주관식(SA) 3종 지원 |
| **정답 체크 안정화** | 객관식은 `answer_index`, OX는 `answer` boolean, 주관식은 `accepted_answers` 기준으로 채점 |
| **가중치 랜덤 출제** | 타입별 균등 배분(MC/OX/SA 3~4개씩) + 오답 문제 3배 가중치 |
| **타임어택 모드** | 문제당 10초 제한, 배점 2배(기본 20점), 남은 시간 보너스(초당 2점) |
| **게임 가이드** | 타이틀 화면에서 모달 팝업으로 모드/Fever/타임어택/문제유형/오답노트/랭킹 규칙 안내 |

---

### 2. 문제 데이터

| 항목 | 내용 |
|------|------|
| **총 문항 수** | **873문제** |
| **문항 유형 구성** | MC 550 + OX 145 + SA 178 |
| **섹션 범위** | XAG101 ~ XAG123, 총 23개 섹션 |
| **데이터 원본** | `XAG_Quiz_RawData_v26.xlsx`를 원본으로 관리하고, 컨버터를 통해 `XAG_Quiz_RawData.js` 생성 |
| **JS 데이터 메타** | `meta.total_questions`, `question_types`, `pools`, `content_hash`, `generated_at`, `source_file` 포함 |
| **객관식 답안** | `answer_index`와 `answer_text`를 함께 저장, 앱에서는 선택 보기와 정답 보기 비교 |
| **OX 답안** | 컨버터에서 `answer_text`의 O/X를 `answer: true/false`로 변환하여 기존 앱 채점 로직과 호환 |
| **주관식 답안** | `option1~4`와 `answer_text`를 합쳐 `accepted_answers`로 저장, 공백/대소문자 무시 매칭 |
| **동의어/표기 차이 대응** | 주관식에서 `18pt`, `18 pt`, `18포인트`, `18`처럼 동일 의미의 복수 답안을 허용 |

---

### 3. 데이터 변환기 / 파이프라인

| 기능 | 설명 |
|------|------|
| **Excel → JS 변환** | RawData 엑셀을 읽어 앱에서 사용하는 `const QUIZ_DATA = {...}` 형식의 JS 파일로 변환 |
| **JS → Excel 변환** | 기존 JS 데이터를 다시 엑셀로 내보내는 역변환 지원 |
| **OX 정답 보강** | `question_type === 'ox'`일 때 `answer_text`를 기준으로 `q.answer` boolean 생성 |
| **주관식 복수 정답 보강** | `question_type === 'short_answer'`일 때 `option1~4 + answer_text`를 `accepted_answers`로 생성 |
| **answer_index 복구** | 객관식에서 `answer_index`가 비어 있거나 잘못된 경우 `answer_text`와 보기 텍스트를 대조해 복구 |
| **미리보기 제공** | 변환 후 meta, option_pools, questions, 전체 JSON 미리보기 탭 제공 |
| **다운로드/복사** | 변환된 `XAG_Quiz_RawData.js` 다운로드 및 클립보드 복사 지원 |
| **현재 기준 파일** | `XAG_Quiz_Converter_fixed_v2.html` / `XAG_Quiz_Converter_latest.html` 계열을 최신 컨버터 기준으로 사용 |

---

### 4. 게임 모드

| 모드 | 설명 |
|------|------|
| **랜덤 10문제** | 선택 섹션에서 타입별 균등 배분 + 오답 가중치 적용, 랭킹 미반영 |
| **전체 풀기** | 선택 섹션의 모든 문제를 풀이, 랭킹 반영 |
| **타임어택 랜덤 10문제** | 선택 섹션에서 문제당 10초 제한, 배점 x2, 랭킹 미반영 |
| **타임어택 전체 풀기** | 선택 섹션의 모든 문제를 문제당 10초 제한으로 풀이, 배점 x2, 랭킹 반영 |
| **오답노트 풀기** | 틀린 문제만 재도전, 섹션별/전체 선택 가능 |
| **전체 문제 풀기** | 전체 섹션을 대상으로 모드 선택 가능. 단, 랭킹 설명에서는 섹션별 전체/타임어택 전체와 혼동되지 않도록 문구 분리 |

---

### 5. 섹션 선택 화면

| 기능 | 설명 |
|------|------|
| **카테고리형 섹션 선택** | 섹션 증가에 따른 긴 스크롤 문제를 줄이기 위해 카테고리 칩 + 카드 그리드 구조 적용 |
| **카테고리 필터** | 전체, 시각/가독성, 청각/오디오, 입력/조작, 인지/UX, 안전/감각, 커뮤니케이션, 문서/지원 |
| **섹션 카드 UI** | `XAG101` 같은 섹션 코드, 섹션명, 간단 설명, 문항 수를 카드 형태로 표시 |
| **전체 문제 카드** | 전체 문항을 대상으로 시작할 수 있는 카드를 상단에 고정 배치 |
| **오답노트 영역 유지** | 기존 오답노트 진입 버튼은 카테고리 섹션과 분리하여 하단에 유지 |
| **반응형 그리드** | 넓은 화면에서는 2열 카드 그리드, 모바일에서는 1열 구조로 표시 |
| **기존 모드 선택 유지** | 섹션 선택 후 기존 랜덤/전체/타임어택/오답노트 모드 선택 흐름으로 이동 |

#### 섹션 카테고리 매핑

| 카테고리 | 섹션 |
|---|---|
| **시각/가독성** | XAG101, XAG102, XAG103 |
| **청각/오디오** | XAG104, XAG105, XAG111 |
| **입력/조작** | XAG107, XAG110 |
| **인지/UX** | XAG106, XAG108, XAG109, XAG112, XAG113, XAG114, XAG115, XAG116 |
| **안전/감각** | XAG117, XAG118, XAG123 |
| **커뮤니케이션** | XAG119, XAG120 |
| **문서/지원** | XAG121, XAG122 |

---

### 6. 타임어택 시스템

| 기능 | 설명 |
|------|------|
| **타이머 바** | 상단에 10초 게이지가 줄어드는 바, 5초 이하 주황, 3초 이하 빨강 깜빡임 |
| **시계 아이콘 shake** | 5초 이하 흔들림 시작, 3초 이하 격렬한 흔들림 + 확대, 정답/시간초과 시 멈춤 |
| **시간초과 오답 처리** | 10초 내 미응답 시 자동 오답 + 정답 표시 |
| **타이머 정지 연출** | 정답 시 바가 초록 번쩍 + 멈춤, 오답 시 빨간색 멈춤 |
| **타임 보너스** | 정답 시 남은 초 × 2점 추가, `TIME BONUS +14` 팝업 2초 표시 |
| **배점 2배** | 기본 20점 × 피버 배수 + 타임 보너스 — 최대 60점+보너스/문제 |

---

### 7. 게임 플레이 이펙트

| 기능 | 설명 |
|------|------|
| **실시간 점수 표시** | 프로그레스 바 아래에 현재 총점 + 정답률 실시간 표시 (글래스모피즘 카드) |
| **정답 미니 컨페티** | 매 정답마다 작은 색종이 12개 터짐 |
| **오답 화면 흔들림** | 틀리면 화면 전체가 좌우로 흔들리는 shake 효과 |
| **오답 붉은 비네팅** | 틀리면 화면 가장자리가 붉게 번쩍 |
| **정답/오답 화면 플래시** | 정답=초록, 오답=빨간 방사형 플래시 |
| **콤보 카운터** | 3, 5, 7, 10, 15, 20 연속 정답 시 `N COMBO!` 팝업 (단계별 색상 변화) |
| **점수 팝업** | 정답 시 획득 점수가 떠올라 사라지는 효과 |
| **피버 팝업** | 피버 진입 시 `FEVER! x2` / `SUPER FEVER! x3` 중앙 팝업 |
| **피버 파티클 폭발** | 피버 진입 시 화면 중앙에서 파티클이 방사형으로 터져나감 |
| **피버 배경 맥동** | 피버=주황, 슈퍼피버=빨강 배경이 은은하게 맥동 |
| **피버 연동 파티클** | 피버 상태에 따라 배경 파티클 색상/속도 변화 |
| **결과 화면 컨페티** | 정답률 80% 이상 시 대형 색종이 효과 (만점=80개, 80%+=40개) |

---

### 8. UI / 비주얼 디자인

| 기능 | 설명 |
|------|------|
| **다크/라이트 모드** | 우측 상단 토글 버튼, localStorage에 설정 저장, 파티클/카드 등 전체 대응 |
| **그라데이션 배경** | 다크 네이비/인디고 그라데이션 (단색 검정 대비 깊이감) |
| **배경 파티클 시스템** | 50개 파티클 + 근접 파티클 간 연결선, Canvas 기반 실시간 렌더링 |
| **글래스모피즘** | 문제 카드, 보기 버튼, 섹션 버튼, 실시간 점수 바에 반투명 유리 효과 |
| **카드형 섹션 UI** | 섹션 선택 화면에 카테고리별 카드 그리드 적용 |
| **카테고리 칩 UI** | 섹션 선택 화면 상단에서 카테고리 필터를 빠르게 전환 |
| **버튼 리플 이펙트** | 클릭 시 물결이 퍼져나가는 효과 |
| **버튼 호버 글로우** | 마우스 오버 시 파란 발광 + 떠오르는 효과 |
| **타이틀 순차 페이드인** | 제목 → 부제 → 통계 → 로그인 → 시작 → 서브 버튼 순서로 등장 |
| **시작 버튼 맥동** | 주기적으로 글로우가 커졌다 작아지는 pulse 효과 |
| **타이틀 아이콘 플로팅** | 아이콘이 위아래로 부드럽게 떠다니는 애니메이션 |
| **타이틀 버튼 레이아웃** | 시작하기(메인) + 랭킹/가이드(서브 가로 배치) |
| **섹션 버튼/card stagger** | 리스트/카드 항목이 시간차를 두고 순서대로 슬라이드인 |
| **문제 슬라이드 전환** | 다음 문제로 넘어갈 때 슬라이드 애니메이션 |
| **해설 슬라이드인** | 해설이 위에서 부드럽게 내려오며 등장 |
| **Q번호 칩 스타일** | 문제 번호가 파란 반투명 배경의 뱃지 형태 |
| **프로그레스 바 글로우** | 진행률 바에 파란 발광 효과 |
| **다음 문제 버튼** | 그라데이션 배경 + 호버 시 떠오르는 효과 |
| **게임 가이드 모달** | 3페이지 슬라이드 구성(모드/점수&Fever/오답노트&랭킹), 좌우 화살표+도트 네비게이션, 슬라이드 전환 애니메이션 |
| **게임 가이드 문구 개선** | 랭킹 설명에서 `전체 873문항`만 의미하는 것으로 오해하지 않도록 `섹션별 전체 풀기`, `섹션별 타임어택 전체 풀기`로 명확화 |

---

### 9. 결과 화면

| 기능 | 설명 |
|------|------|
| **등급 뱃지** | S(만점) / A(90%+) / B(70%+) / C(50%+) / D — 회전하며 바운스 등장 + 빛 폭발 |
| **S등급 금빛 맥동** | 만점 시 뱃지가 지속적으로 금색 글로우 애니메이션 |
| **점수 카운트업** | 정답 수와 총점이 0부터 올라가는 애니메이션 (ease-out) |
| **스코어 링** | SVG 원형 게이지가 정답률에 맞춰 채워지는 애니메이션 + 글로우 |
| **Fever/타임어택 보너스 표시** | 기본 점수 + Fever 보너스 + 타임어택 배점 내역 표시 |
| **결과 화면 진입** | scale 0.9 → 1로 확대되며 등장 |
| **랭킹 이동** | 결과 화면에서 현재 섹션 기준 랭킹 화면으로 이동 가능 |

---

### 10. 인프라

| 항목 | 내용 |
|------|------|
| **배포** | GitHub Pages, main 브랜치 push 시 자동 배포 |
| **구조** | 단일 HTML + 외부 JS 데이터 파일, 빌드 도구 없음 |
| **Firebase** | Auth(Google 로그인) + Firestore(점수, 랭킹, 오답노트) |
| **데이터 파이프라인** | Excel → Converter(HTML) → JS, v26 기준 OX/주관식까지 컨버터에서 자동 처리 |
| **현재 앱 파일 기준** | `index_section_categories_guidefix.html`의 내용을 기존 `index.html`로 교체하여 적용 |
| **현재 데이터 파일 기준** | v26 변환 완료된 `XAG_Quiz_RawData.js` 사용 |
| **현재 컨버터 기준** | `XAG_Quiz_Converter_fixed_v2.html` 또는 `XAG_Quiz_Converter_latest.html` 사용 |

---

### 11. 최근 개선 이력 요약

| 구분 | 개선 내용 |
|------|------|
| **데이터 확장** | 133문항에서 v26 기준 873문항으로 확장, XAG101~XAG123 전체 섹션 반영 |
| **OX 정답 체크 수정** | 컨버터가 `answer_text` O/X를 `answer: true/false`로 변환하도록 개선 |
| **주관식 정답 체크 수정** | 단답형의 option1~4를 복수 정답 후보로 사용하도록 `accepted_answers` 자동 생성 |
| **앱 로직 유지** | 기존 `index.html`의 채점 로직은 유지하고 데이터 생성 방식을 보강하여 호환성 확보 |
| **섹션 선택 UI 개선** | 섹션 증가에 맞춰 카테고리 칩 + 카드 그리드 방식으로 스크롤 부담 완화 |
| **랭킹 문구 개선** | 게임 가이드와 랭킹 설명에서 섹션별 전체 풀기/섹션별 타임어택 전체 풀기만 랭킹 반영됨을 명확화 |
| **배포 주의점 정리** | GitHub Pages 캐시/반영 지연 가능성을 고려해 JS 교체 후 강제 새로고침/캐시 확인 필요 |
